# Pin noise and peripheral scatter

Later focused Tier 3 renderer/control change on the existing stage-1 branch. User explicitly preserves Pointer. Plan routes (controls, renderer, timeline, reference/visual mismatch) have been read in this conversation; retain existing runtime, scene sizing, media, timeline, layers-off and export decisions.

Replace the synchronized branching flash with deterministic independent cell color noise. Controls in the existing pins.items collection: Position, Color, Radius, Color mix, Noise amount, Noise speed, Soft edge, Scatter. No new section or finite selector. Radius/position/color persist; retired phase controls are ignored and new fields get schema defaults. Noise speed zero freezes the noise. Scatter zero leaves exact cell coordinates while color noise remains. Soft edge attenuates color and motion without raster blur. Preserve the center: scatter has zero center influence and increases at the periphery, using independent signed offsets instead of radial repulsion. No empty mask cells or particle duplication.

Implement in pin-animation.ts, interaction-controls.ts (Pins only), parsePins in dither-scene.ts, Pin sampling and offset addition in dither-renderer.ts. Preserve pointer-physics.ts, Pointer controls, hooks and DitherCanvas. Both preview and deterministic export share Pin noise on runtime loop progress; per-pin normalized coordinates make noise stable across backing scales. Existing source caches and pipeline lifecycle remain. Work is O(lit cells * pins), bounded scalar noise with no new allocations per retained particle or feedback buffers; no GPU migration or measurement authority.

Update Pin acceptance prose and focused tests. Verify independent local temporal/color variation, soft boundary, zero scatter, intact center, increasing scatter spread, deterministic loop and scale parity, exact unchanged outside region, and additive Pointer offsets. Run focused Pin/renderer/export/schema unit tests, pins.items and persistence.reload browser acceptance, plus existing pointer.enabled browser regression because the shared renderer adds offsets. Typecheck is justified by the changed shared DitherPin type. No full build/audit/benchmarks or protected drag gate retry. Use existing available headless Playwright fallback for diagnostic visual captures if embedded CUA remains unavailable.
